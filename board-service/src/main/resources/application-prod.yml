server:
  port: 8080

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${BOARD_DB_URL}
    username: ${BOARD_DB_USERNAME}
    password: ${BOARD_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}

client:
  user-service:
    url: ${USER_SERVICE_URL}
  point-service:
    url: ${POINT_SERVICE_URL}
  api-gateway-service:
    url: ${API_GATEWAY_SERVICE_URL}

logging:
  level:
    root: INFO
    woobl0g.boardservice: INFO
  file:
    name: logs/board-service.log
    path: logs
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 30
      total-size-cap: 1GB